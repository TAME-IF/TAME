- Action type fallback.

Add keywords:
	archetype
	
+TElement
	archetype : boolean <-- not counted as "real" objects - they are the basis for other objects. (compiler guideline)
	isArchetype() : boolean
